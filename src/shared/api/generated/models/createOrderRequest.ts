/**
 * Generated by orval v8.10.0 🍺
 * Do not edit manually.
 * Lyceum 157 Hub API
 * REST API for Lyceum 157 Hub — a B2C marketplace where minor students sell
their projects. The platform acts as a Ukrainian sole-proprietor (FOP) tax
agent: it collects payments via LiqPay, fiscalizes via Checkbox PRRO,
withholds PDFO 18% + ВЗ 1.5%, and pays NET to parents' cards.

**Conventions:**
- Errors follow RFC 7807 `application/problem+json`.
- All mutating endpoints require an `Idempotency-Key` header.
- All money values are in UAH with `DECIMAL(10,2)` precision.
- All timestamps are ISO-8601 UTC.

 * OpenAPI spec version: v1
 */
import type { DeliveryRequest } from "./deliveryRequest";
import type { OrderItemRequest } from "./orderItemRequest";

/**
 * Buyer details + cart. The Idempotency-Key header guards against double submissions.
 */
export interface CreateOrderRequest {
  /**
   * Buyer email (receipt destination)
   * @minLength 0
   * @maxLength 255
   */
  buyerEmail: string;
  /**
   * Buyer full name
   * @minLength 0
   * @maxLength 255
   */
  buyerName: string;
  /**
   * Buyer phone in +380 format
   * @pattern ^\+380\d{9}$
   */
  buyerPhone: string;
  /** Cart items (at least one) */
  items: OrderItemRequest[];
  /** reCAPTCHA v3 token (required when app.captcha.enabled=true). The server verifies score; below threshold orders are rejected with 403. */
  recaptchaToken?: string;
  delivery?: DeliveryRequest;
}
