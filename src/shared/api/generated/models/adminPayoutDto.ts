/**
 * Generated by orval v8.10.0 🍺
 * Do not edit manually.
 * Lyceum 157 Hub API
 * REST API for Lyceum 157 Hub — a B2C marketplace where minor students sell
their projects. The platform acts as a Ukrainian sole-proprietor (FOP) tax
agent: it collects payments via LiqPay, fiscalizes via Checkbox PRRO,
withholds PDFO 18% + ВЗ 1.5%, and pays NET to parents' cards.

**Conventions:**
- Errors follow RFC 7807 `application/problem+json`.
- All mutating endpoints require an `Idempotency-Key` header.
- All money values are in UAH with `DECIMAL(10,2)` precision.
- All timestamps are ISO-8601 UTC.

 * OpenAPI spec version: v1
 */

/**
 * Admin row for the payout queue
 */
export interface AdminPayoutDto {
  /** Payout UUID */
  payoutId?: string;
  /** Owning student UUID */
  studentId?: string;
  /** Student full name (first + last) */
  studentFullName?: string;
  /** Student grade */
  studentGrade?: string;
  /** Parent name from KYC */
  parentName?: string;
  netAmount?: string;
  /** Masked card, e.g. "**** 1234" */
  cardMasked?: string;
  /** Frontend-facing status: HOLD|APPROVED|PROCESSING|PAID|FAILED|CANCELLED */
  status?: string;
  /** When admin or scheduler approved */
  approvedAt?: string;
  /** When this payout is scheduled to execute (= hold_until for HOLD) */
  scheduledAt?: string;
  /** When LiqPay confirmed the transfer; null until PAID */
  paidAt?: string;
  /** Last failure reason; null if not failed */
  failureReason?: string;
  /** External LiqPay payout reference; null until PROCESSING */
  liqpayPayoutId?: string;
  /** Retries remaining (cap is 3) */
  retriesLeft?: number;
}
