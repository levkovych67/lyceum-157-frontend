/**
 * Generated by orval v8.10.0 🍺
 * Do not edit manually.
 * Lyceum 157 Hub API
 * REST API for Lyceum 157 Hub — a B2C marketplace where minor students sell
their projects. The platform acts as a Ukrainian sole-proprietor (FOP) tax
agent: it collects payments via LiqPay, fiscalizes via Checkbox PRRO,
withholds PDFO 18% + ВЗ 1.5%, and pays NET to parents' cards.

**Conventions:**
- Errors follow RFC 7807 `application/problem+json`.
- All mutating endpoints require an `Idempotency-Key` header.
- All money values are in UAH with `DECIMAL(10,2)` precision.
- All timestamps are ISO-8601 UTC.

 * OpenAPI spec version: v1
 */
import type { StudentOrderItemDtoPayoutStatus } from "./studentOrderItemDtoPayoutStatus";
import type { StudentOrderItemDtoStatus } from "./studentOrderItemDtoStatus";

/**
 * Single sold line-item from the student's catalog of products
 */
export interface StudentOrderItemDto {
  /** Order item UUID */
  orderItemId?: string;
  /** Human-readable order number */
  orderNumber?: string;
  /** Product UUID */
  productId?: string;
  /** Product title (snapshot at order time) */
  productTitle?: string;
  /** Primary image CDN URL; null if product has no images */
  productThumbnailUrl?: string;
  /** Number of units in this line */
  quantity?: number;
  pricePerUnit?: string;
  grossAmount?: string;
  /** Order state — only PAID/FULFILLED/DELIVERED/REFUNDED/DISPUTED appear */
  status?: StudentOrderItemDtoStatus;
  /** When the order was paid */
  paidAt?: string;
  /** Synthesised payout state for the UI */
  payoutStatus?: StudentOrderItemDtoPayoutStatus;
  /** When HOLD ends; null unless payoutStatus=HOLD */
  payoutReleasesAt?: string;
  /** Buyer name masked for privacy, e.g. "Анна П." */
  buyerNameMasked?: string;
}
