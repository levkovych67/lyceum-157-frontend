/**
 * Generated by orval v8.10.0 🍺
 * Do not edit manually.
 * Lyceum 157 Hub API
 * REST API for Lyceum 157 Hub — a B2C marketplace where minor students sell
their projects. The platform acts as a Ukrainian sole-proprietor (FOP) tax
agent: it collects payments via LiqPay, fiscalizes via Checkbox PRRO,
withholds PDFO 18% + ВЗ 1.5%, and pays NET to parents' cards.

**Conventions:**
- Errors follow RFC 7807 `application/problem+json`.
- All mutating endpoints require an `Idempotency-Key` header.
- All money values are in UAH with `DECIMAL(10,2)` precision.
- All timestamps are ISO-8601 UTC.

 * OpenAPI spec version: v1
 */
import type { AdminOrderListDtoStatus } from "./adminOrderListDtoStatus";

/**
 * Admin row for order list
 */
export interface AdminOrderListDto {
  /** Order UUID */
  orderId?: string;
  /** Human-readable order number */
  orderNumber?: string;
  /** Order state */
  status?: AdminOrderListDtoStatus;
  totalAmount?: string;
  /** Buyer email */
  buyerEmail?: string;
  /** Buyer name (NOT masked for admin) */
  buyerName?: string;
  /** Number of order items */
  itemsCount?: number;
  /** When the order was paid; null if not yet paid */
  paidAt?: string;
  /** Latest moment a refund can still be initiated; null if not refundable now */
  refundableUntil?: string;
}
